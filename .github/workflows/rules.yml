# SPDX-License-Identifier: AGPL-3.0-or-later
#
# Enforce the repository rules that are checkable without a toolchain.
#
# CONTRIBUTING.md tells contributors the SPDX header rule "is linted in CI, so a
# missing or mismatched header fails the build rather than being caught in
# review", and CLAUDE.md says rule SCOPE001 "fails the build". This workflow is
# what makes those statements true. Until it existed they were not.
#
# The first half of the job runs on a bare clone with bash and coreutils and
# needs no toolchain at all. The second half is the workspace gate #23
# established -- install, format, lint, typecheck, test and build. They share one
# job deliberately: `main` requires a status check whose context is exactly
# "Repository rules", so a second job would report under a different context and
# a toolchain failure would not block a merge.
#
# Since #63 the job also drives a real browser. That is the one thing the rest
# of the suite cannot do: jsdom implements no WebGL, so MapLibre cannot be
# constructed there at all, and "which hosts does the map engine actually
# contact" is a question only a browser answers.
#
# Still not here, and owned by the second half of #24: the per-package
# dependency-licence allowlist, and a coverage gate demonstrated to fail.
name: Repository rules

# `pull_request` runs in the fork's context with a read-only token and no access
# to secrets. Its target-context counterpart -- which does receive secrets and is
# not covered by the fork-approval gate at all -- is banned by CLAUDE.md section
# 8 and enforced by rule WF001 in scripts/check-repo-rules.sh. That rule greps
# this directory for the name, so the name cannot appear here even in a comment.
on:
  pull_request:
  push:
    branches: [main]

# Set explicitly and minimally at workflow level: every step below reads the
# checked-out tree and writes nothing back.
permissions:
  contents: read

# Supersede an in-flight run when a PR is pushed again. At ~7 seconds a run this
# saves nothing today; it is here so the habit predates the toolchain half of
# #24, when a run becomes long enough for two to overlap and the stale one to
# report against code that no longer exists.
#
# Pull requests only. On `main` every commit's run is a record of whether that
# commit was green, and two merges in quick succession would otherwise cancel
# the first -- leaving a commit on the default branch with no completed run
# against it, which is the one place the history matters.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  rules:
    name: Repository rules
    # Standard label only. Larger runners (`4-core`, `8-core`, any `larger`
    # label) are charged even on a public repository, while standard
    # GitHub-hosted runners are free and unlimited -- CLAUDE.md section 8. It is
    # a one-word diff that produces an invoice and nothing in the log warns you.
    runs-on: ubuntu-latest
    # A stop on a hung job rather than a budget; the platform default would let
    # one run for six hours. Raised from 10 with #63: the browser gate downloads
    # a pinned Chromium (~170 MB), which is fast on a good day and is the one
    # step here whose duration depends on somebody else's CDN.
    #
    # Raised from 15 with #423. A stop has to be clear of the thing it stops, and
    # it no longer was: the last three runs on main took 9m20s, 12m55s and 13m05s,
    # and the first run of #423's pull request ran for 14m19s with nothing hung
    # -- 41 seconds from being killed. The browser gate is what grew (6.8 minutes on
    # main, 8.0 with the ride-stage and Ride-screen specs), and every case in
    # game.browser.spec.ts reloads a harness that takes ten seconds on a runner
    # with no GPU. Twenty is still a stop: a hung job dies in twenty minutes
    # rather than six hours.
    timeout-minutes: 20
    steps:
      - name: Check out the repository
        # Pinned to a full commit SHA, not a tag: a tag is mutable, and a
        # compromised action runs with whatever permissions this job holds.
        # Pinned to a full commit SHA, not a tag: tags are mutable, and a
        # retagged action runs with this job's permissions. Provenance, so a
        # reviewer can check rather than trust:
        #   gh api repos/actions/checkout/git/ref/tags/v7.0.1 --jq .object.sha
        #   -> 3d3c42e5aac5ba805825da76410c181273ba90b1   (verified 2026-09-03)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Nothing here pushes, so the token has no reason to survive in
          # .git/config where a later step could reach it.
          persist-credentials: false

      # CLAUDE.md section 4a. Every command below is one that file documents;
      # CI accumulating private knowledge is how a contributor's local green
      # becomes CI's red with no explanation.
      - name: Repository rules (LIC001-4, LIC006, SCOPE001, WF001, ADR001-3)
        run: bash scripts/check-repo-rules.sh

      - name: Repository rule checker tests
        run: bash scripts/check-repo-rules.test.sh

      # A silent edit to a licence text is a licensing incident, not a typo, and
      # it is the shape of diff a human reviewer scrolls past.
      - name: Licence text integrity (LIC005)
        run: bash scripts/check-licence-hashes.sh

      - name: Licence integrity checker tests
        run: bash scripts/check-licence-hashes.test.sh

      - name: Environment template completeness (ENV001)
        run: bash scripts/check-env-example.sh

      - name: Environment template checker tests
        run: bash scripts/check-env-example.test.sh

      # DOC001/DOC002. Relative links only — an external URL is not checked,
      # because that would need the network and turn a bare-clone gate into one
      # that fails on an aeroplane. See CLAUDE.md §4a.
      - name: Documentation links (DOC001, DOC002)
        run: bash scripts/check-doc-links.sh

      - name: Documentation link checker tests
        run: bash scripts/check-doc-links.test.sh

      # shellcheck is preinstalled on the standard Ubuntu runner image
      # (0.9.0 on ubuntu-24.04, which `ubuntu-latest` currently resolves to), so
      # there is nothing to install. The version is printed because the runner's
      # is older than a typical developer's and the two can disagree.
      - name: Shell lint
        run: |
          shellcheck --version
          shellcheck scripts/*.sh

      # --- The workspace toolchain, from here down (CLAUDE.md section 4a) ------

      # Reads .nvmrc so the runner and a contributor cannot disagree about the
      # major version. ADR 0005 pins Node 24 "Krypton"; Node 26 becomes Active
      # LTS on 2026-10-28 and the move happens on that date, not before.
      - name: Set up Node
        # Pinned to a full commit SHA, not a tag. Provenance, so a reviewer can
        # check rather than trust:
        #   gh api repos/actions/setup-node/git/ref/tags/v7.0.0 --jq .object.sha
        #   -> 820762786026740c76f36085b0efc47a31fe5020   (verified 2026-09-03)
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc

      # pnpm comes from the `packageManager` field via corepack rather than from
      # a third-party setup action, so the version is pinned by the manifest that
      # a contributor also reads and there is one less action to trust.
      #
      # No dependency cache: it would mean a second action to pin and a writable
      # cache in the dependency path of every job, to save a couple of seconds on
      # a workspace this size. Revisit when the install is slow enough to notice.
      - name: Enable pnpm through corepack
        env:
          COREPACK_ENABLE_DOWNLOAD_PROMPT: '0'
        run: |
          corepack enable pnpm
          pnpm --version

      # --frozen-lockfile: fail rather than silently resolve something the
      # committed lockfile does not describe. pnpm 11 also verifies every
      # lockfile entry against its supply-chain policies here.
      #
      # This step runs the install described by the *fork's* checkout on a pull
      # request, so `pnpm-workspace.yaml`'s `allowBuilds` block is a
      # security-relevant file: flipping an entry to `true` is what lets a
      # dependency's install script execute here. What keeps that acceptable is
      # already in place -- `pull_request` rather than its target-context
      # counterpart, `permissions: contents: read`, no secrets in this job,
      # `persist-credentials: false`, and GitHub's first-time-contributor
      # approval gate. Read that block on every fork pull request anyway
      # (CLAUDE.md section 8).
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Formatting
        run: pnpm run format:check

      - name: Lint
        run: pnpm run lint

      - name: Typecheck
        run: pnpm run typecheck

      # `test:coverage`, not `test`. ADR 0005 decision C sets no percentage
      # floor and forbids inventing one -- the gate is the mutation list in the
      # pull request body. The report is here because an untested branch is
      # worth seeing in review, and because it costs nothing to print.
      - name: Tests and coverage report
        run: pnpm run test:coverage

      # #48's fourth acceptance criterion: automated accessibility checks run on
      # every route and fail the build on a violation. The checks are Vitest
      # tests, so the step above already ran them and this one runs them again
      # -- deliberately. A failure buried among two hundred other cases is a
      # failure somebody scrolls past; under a step called "Accessibility" it is
      # not. The whole set takes under a second.
      #
      # In this job rather than a job of its own: `main` requires a status check
      # whose context is exactly "Repository rules", and a second job would
      # report under a different one and could not block a merge (CLAUDE.md
      # section 4c).
      #
      # The filter is a path substring, and until #142 it was the DIRECTORY
      # name. That failed closed against deleting apps/web/src/a11y/ -- Vitest
      # exits non-zero when a filter selects no files -- and OPEN against
      # renaming it: renaming to src/accessibility/ matched nothing named
      # `a11y`, dropped audit.test.ts (the suite proving every rule actually
      # fires) and exited 0 with 6 files and 61 tests instead of 7 and 101.
      # Measured on this branch, not reasoned about.
      #
      # It is now `.a11y.test.`, which matches the FILENAME. No directory rename
      # can drop a file out of the gate, and a file added under the convention
      # is picked up without editing this workflow.
      #
      # The check that runs first closes the remaining hole: a genuine
      # accessibility test named without the convention would be silently
      # outside the gate, so check-a11y-suite.mjs compares what Vitest selects
      # against what is on disk and fails on a difference. It runs BEFORE the
      # gate, so a mis-scoped gate is reported as that rather than as a green
      # run over fewer files.
      - name: Accessibility
        run: |
          pnpm run check:a11y-suite
          pnpm run test:a11y

      # The checker above decides what counts as an accessibility test from
      # paths and from package.json's own script, so it has a suite, and the
      # suite runs here. Same reasoning as the coverage renderer below: a check
      # that silently stopped checking is indistinguishable from a clean tree.
      #
      # Not part of `pnpm run check:repo`: that is the bare-clone set, and this
      # one needs Node.
      - name: Accessibility gate checker tests
        run: bash scripts/check-a11y-suite.test.sh

      # The wiring gate (#278). Every gate above this one looks INSIDE a unit,
      # and five defects in one week were a correct, unit-tested, typechecked,
      # linted unit wired to nothing: no device could be paired on Android at
      # all (#230), there was no pacer (#237), a rider who beat their own best
      # was never told (#259). An exported function nobody calls and an optional
      # parameter nobody supplies are both perfectly well typed, so neither the
      # typechecker nor `no-unused-vars` can see one.
      #
      # This walks the module graph from the page Vite builds and reports what
      # the client's own seams cannot reach. It needs an install (it parses with
      # the TypeScript compiler's own parser), so it is NOT in `check:repo`.
      #
      # In this job rather than one of its own, for the reason the accessibility
      # steps above give: `main` requires a status check whose context is
      # exactly "Repository rules".
      - name: Wiring
        run: pnpm run check:wiring

      # Its own suite, for the reason every checker here has one: a gate that
      # silently stopped checking is indistinguishable from a clean tree. Five
      # of its cases are #278's five defects, reproduced from the tree as it
      # actually was, and two of those are green on purpose and named so --
      # a stated limit nobody has pinned is a limit that quietly becomes a false
      # claim.
      - name: Wiring gate checker tests
        run: bash scripts/check-wiring.test.sh

      # The generated-artefact gate (#299). `cap sync` writes two files this
      # repository commits, and pnpm's store path carries the PEER version as
      # well as the package's own -- so any Capacitor bump invalidates
      # capacitor.settings.gradle, and #276/#277 did exactly that. Nothing could
      # see it: CI never builds Android, release.yml is tag-triggered and has
      # never run, and the documented local sequence regenerates the file before
      # Gradle reads it, so the person most likely to notice is the one whose
      # workflow guarantees they cannot.
      #
      # This regenerates with `cap update android` and fails if either file
      # changes. It runs its own `pnpm install --frozen-lockfile` first, and that
      # is not ceremony: #298 regenerated from a node_modules nobody had checked
      # against the lockfile and committed a DIFFERENT wrong path. A no-op here,
      # a few hundred milliseconds, because the install step above already ran.
      #
      # ⚠️ It does NOT need the build, and the reason is not obvious: `cap
      # update` falls back to a full `copy` when the copied-build directory
      # is missing, and `copy` refuses to start without apps/web/dist. Both
      # CI runs of this gate's first two commits died on exactly that, in two
      # different ways, because a developer's tree always has the gitignored
      # directories a fresh checkout does not. The checker creates them and
      # removes them again -- see GENERATED_DIRECTORIES before moving this
      # step or concluding it belongs after Build.
      #
      # It needs an install, so it is NOT in `check:repo`, the bare-clone set.
      # In this job rather than one of its own, for the reason every step above
      # gives: `main` requires a status check whose context is exactly
      # "Repository rules".
      - name: Capacitor generated files
        run: pnpm run check:capacitor

      # Its own suite, for the reason every checker here has one. Two of its red
      # cases are the drift that actually happened, reproduced from the tree as
      # it was -- including #298's own artefact, where the `core` half of the
      # path is right and the `android` half is wrong, which is what a check
      # comparing one dependency or stopping at the first path would pass.
      - name: Capacitor gate checker tests
        run: bash scripts/check-capacitor-generated.test.sh

      # The cost model (#54). docs/cost-model.md states its inputs with their
      # provenance and states what follows from them; this recomputes the second
      # from the first and fails when they disagree. It is the gate for a defect
      # no other one here can see: arithmetic that was true on the day it was
      # typed, in a document whose inputs move. Two of them moved while it was
      # being written -- the Protomaps planet build grew 103 MB in four days,
      # and Verisign has announced a `.com` rise for 1 November 2026.
      #
      # It needs Node but no dependencies; it is nevertheless not in
      # `check:repo`, which is the bare-clone set and is bash and coreutils
      # only. In this job rather than one of its own, for the reason every step
      # above gives: `main` requires a status check whose context is exactly
      # "Repository rules".
      - name: Cost model (COST001-6)
        run: pnpm run check:cost-model

      # Its own suite, for the reason every checker here has one. Its most
      # important case is the vacuous pass: a document whose tables parse and
      # are never actually compared against anything, which without a guard
      # exits 0 with a success line saying the model checks out.
      - name: Cost model checker tests
        run: bash scripts/check-cost-model.test.sh

      # The renderer has logic of its own -- it decides which package a covered
      # file belongs to, from absolute paths that differ between a checkout, a
      # runner and a worktree -- so it has a suite, and the suite runs here. Its
      # first version attributed a dependency's own `packages/<name>/` directory
      # to a workspace package of that name; the suite caught it.
      #
      # Not part of `pnpm run check:repo`: that is the bare-clone set, and this
      # one needs Node.
      - name: Coverage renderer tests
        run: bash scripts/coverage-summary.test.sh

      # Coverage is REPORTED, never gated -- ADR 0005 decision C and CLAUDE.md
      # §5 make the mutation list the gate and forbid a percentage floor. This
      # step exists because the numbers were being computed and then thrown
      # away: `text` goes into this log under the install and build output, and
      # `html` is written to a runner that is destroyed with it.
      #
      # `if: always()` so a failing suite still publishes what it measured --
      # the run where coverage moved unexpectedly is the one you want the table
      # for. It cannot fail the job: the renderer exits non-zero only when the
      # report is missing or unparseable, and `|| true` keeps even that from
      # turning a reporting step into a gate.
      - name: Publish the coverage summary
        if: always()
        run: node scripts/coverage-summary.mjs >> "$GITHUB_STEP_SUMMARY" || true

      # The full HTML report, for the run where a number in the table above
      # looks wrong and the question is which line.
      #
      # SHA from:
      #   gh api repos/actions/upload-artifact/git/ref/tags/v7.0.1 --jq .object.sha
      - name: Upload the coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-report
          path: coverage/
          retention-days: 7
          if-no-files-found: warn

      # A green typecheck is not a green build: the bundler resolves imports the
      # typechecker only reads types from.
      - name: Build
        run: pnpm run build

      # --- The browser gate (#63) ----------------------------------------------

      # ⚠️ The runner image pre-configures Google's own Chrome apt repository for
      # the Chrome it ships, and `--with-deps` runs `apt-get update` across EVERY
      # configured source before it installs anything -- so one unrelated
      # publisher having a bad afternoon fails this step. On 2026-09-09 that
      # repository served a `Release` file regenerated at 17:16 UTC alongside a
      # `Packages.gz` last modified at 09:41, and apt refused the mismatch:
      #
      #   Err https://dl.google.com/linux/chrome-stable/deb stable/main amd64 Packages
      #     Hash Sum mismatch
      #   E: Failed to fetch .../binary-amd64/Packages.gz  Hash Sum mismatch
      #   Failed to install browsers
      #   Error: Installation process exited with code: 100
      #
      # It failed byte-identically on a re-run, so it is not a mirror mid-sync
      # that settles on its own, and it would fail the same way on `main`: the
      # broken source is in the image, not in any diff.
      #
      # Nothing here uses that repository. The browser under the gate is the one
      # `@playwright/test` pins, fetched from Playwright's own CDN by the step
      # below, and every shared library `--with-deps` installs comes from
      # Ubuntu's archives. So dropping the source removes a third-party
      # publisher from the set this job trusts *as well as* unblocking it --
      # after this step CI depends on strictly less than it did before.
      #
      # Both spellings, because the image names it `.list` today and apt's
      # deb822 format would name it `.sources`; `rm -f` because a missing file
      # is this step's desired outcome rather than an error.
      - name: Drop the unrelated Chrome apt source
        run: |
          sudo rm -f /etc/apt/sources.list.d/google-chrome.list \
                     /etc/apt/sources.list.d/google-chrome.sources

      # Chromium only, and pinned by the lockfile: @playwright/test 1.63.0 ships
      # Chromium revision 1243 and `playwright install` fetches exactly that, so
      # the browser is as reproducible as the toolchain above. (#206 took this
      # from 1.56.0/1194; CLAUDE.md section 4c requires these two numbers to move
      # together and to be updated in the same pull request as the bump.)
      #
      # `--with-deps` installs the shared libraries a headless Chromium needs on
      # a bare runner. It uses sudo, which ubuntu-latest grants passwordless.
      #
      # This is the slowest step in the job by a wide margin -- a ~170 MB
      # download against a five-second test run. It buys the only assertions in
      # the repository that a real engine makes: that MapLibre initialises
      # against the style we build, that `addProtocol` takes effect, and that the
      # map contacts no host but the configured basemap origin. `map/port.ts`
      # records why everything else is behind a seam.
      #
      # Why not a job of its own, when a slow step in a fast job is exactly what
      # a separate job is for: `main` requires a status check whose context is
      # exactly "Repository rules", and a second job reports under a different
      # one and could not block a merge (CLAUDE.md section 4c). A gate that
      # cannot block is not a gate.
      - name: Install the pinned browser
        run: pnpm --filter @onyourleft/web exec playwright install --with-deps chromium

      # Builds the harness and drives it. CLAUDE.md section 4a documents both
      # halves as one command.
      - name: Browser gate
        run: pnpm run test:browser

      # TEMPORARY (#552, #568): one repeat run in CI. Reverted before review.
      - name: TEMPORARY sidelink repeat
        run: pnpm --filter @onyourleft/web exec playwright test sidelink --repeat-each=150

      # DEP001. The other half of the licence boundary: check-repo-rules.sh
      # checks the headers and manifests of the code we write, this checks the
      # licence of the code we pull in. It reads pnpm's own resolution of its
      # own lockfile, so it needs the install above -- which is why it is here
      # and not in check:repo, the bare-clone set. ADR 0015 classifies the
      # licences; CLAUDE.md section 4g says what it does and does not cover.
      - name: Dependency licence checker tests
        run: bash scripts/check-dependency-licences.test.sh

      - name: Dependency licences
        run: pnpm run check:licences
