{
  "name": "@onyourleft/fit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "FIT / GPX / TCX codec. Today: the synthetic fixture corpus, its generator and the ADR 0004 decision G region guard.",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit --project tsconfig.json && tsc --noEmit --project tsconfig.platform-free.json",
    "test": "vitest run",
    "fixtures:generate": "node --import ./tools/ts-extensionless-resolve.mjs tools/fixture-corpus/generate.ts"
  },
  "dependencies": {
    "@onyourleft/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "4.1.11"
  }
}
