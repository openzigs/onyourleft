{
  "name": "@onyourleft/store",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "Local activity store \u2014 athletes, activities, laps and per-second streams in IndexedDB via Dexie, the migration up/down pairs, and the round-trip persistence harness.",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@onyourleft/domain": "workspace:*",
    "dexie": "4.4.5"
  },
  "devDependencies": {
    "fake-indexeddb": "6.2.5",
    "typescript": "6.0.3",
    "vitest": "4.1.11"
  }
}
